# Fonts

Creating aliases for `fonts` in your `theme` is the same as `theme-ui`.

However, using custom fonts with Dripsy is very specific to React Native.

## Using Custom Fonts

Please reference the [Custom Fonts Guide](/fonts) for more.
